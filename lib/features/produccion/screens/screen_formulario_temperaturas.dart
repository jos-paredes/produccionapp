import 'package:flutter/material.dart';

class FormularioTemperaturasScreen extends StatelessWidget {
  const FormularioTemperaturasScreen({super.key});
  @override
  Widget build(BuildContext context) => Scaffold(body: Center(child: Text('temperaturas')));
}